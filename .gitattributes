# 统一按 LF 存储，避免 Windows/Linux 协作时行尾混乱
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jar binary
*.keystore binary
*.apk binary

# Windows 脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
